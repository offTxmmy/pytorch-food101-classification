# End-to-End Food Image Classification with PyTorch

An end-to-end computer vision project built with PyTorch using the Food-101 dataset.

The project will cover the complete deep learning workflow, including data pipelines, custom CNN training, GPU acceleration, evaluation, transfer learning, and inference.